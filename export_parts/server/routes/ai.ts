import { Router } from "express";

const router = Router();

// Placeholder for future AI implementation
router.get('/status', (req, res) => {
  res.json({ status: 'Service not available' });
});

export default router;